streamlit>=1.30
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
# Optional: enables XGBoost ML classifier in Tracker tab.
# If not installed, falls back to a built-in logistic regression.
xgboost>=2.0
# Optional: enables Turso (hosted SQLite) for persistent scan history.
# If TURSO_URL + TURSO_TOKEN are set in secrets, scan history survives
# redeploys. If unset or libsql can't load, falls back to local SQLite.
libsql-experimental>=0.0.40
